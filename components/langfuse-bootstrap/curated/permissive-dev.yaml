# Permissive-dev overlay — the local-development default profile.
#
# Only strictly needed for the harness to render; the point is to
# prove the merge system works. Real overlays live in the
# regulatory profiles (eu-ai-act-high-risk, nist-ai-rmf, …).

saved_views:
  - name: harness-smoke
    description: Traces emitted by examples/harness-smoke.
    filters:
      event_class: decision_summary
      fabric.tenant_id: harness
