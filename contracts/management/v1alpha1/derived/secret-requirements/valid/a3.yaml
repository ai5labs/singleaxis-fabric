apiVersion: fabric.singleaxis.dev/v1alpha1
kind: FabricSecretRequirements
metadata:
  name: payments-agent-eu-secrets
status: unresolved
requirements:
  - name: fabric-exporter-client
    namespace: singleaxis-fabric
    keys:
      - tls.crt
      - tls.key
    purpose: exporter-mtls
    consumer: fabric-relay
