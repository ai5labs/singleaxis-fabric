Fabric Relay {{ .Chart.AppVersion }}

Mode: {{ .Values.mode }}
{{- if eq .Values.mode "development" }}

WARNING: development mode is not an audit trail.{{ if not .Values.destination.endpoint }} Telemetry is written only to pod logs.{{ end }}
The queue uses ephemeral storage unless persistence.enabled is explicitly set.
Do not represent this installation as durable or production-qualified.
{{- else }}

Production safety checks passed at render time:
  - OTLP/gRPC and OTLP/HTTP require client-certificate authentication
  - ingress is restricted to explicit NetworkPolicy selectors
  - destination is explicitly allowlisted
  - TLS verification is enabled
  - debug export is disabled
  - a persistent volume claim is provisioned per replica
  - retries do not expire

These checks do not prove runtime storage durability or destination acknowledgement
semantics. Run the outage/recovery qualification in the chart README.
{{- end }}

OTLP/gRPC: {{ if .Values.receiver.tls.serverCertificateSecret.name }}mTLS{{ else }}plaintext development{{ end }} on {{ include "fabric-relay.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.ports.otlpGrpc }}
OTLP/HTTP: {{ if .Values.receiver.tls.serverCertificateSecret.name }}https{{ else }}http{{ end }}://{{ include "fabric-relay.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.ports.otlpHttp }}
Metrics:   http://{{ include "fabric-relay.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.ports.metrics }}/metrics
